apiVersion: 1

providers:
  - name: syslog-ml
    orgId: 1
    folder: "Syslog ML"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
